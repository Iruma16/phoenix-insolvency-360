# Phoenix Legal — UI (mini guía)

La documentación principal está en `README.md`. Aquí solo lo mínimo para arrancar la UI.

## Ejecutar

En dos terminales:

```bash
make run-api
make run-ui
```

Por defecto, Streamlit abre en `http://localhost:8501`.


